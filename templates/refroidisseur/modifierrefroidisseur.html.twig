{% extends 'base.html.twig' %}

{% block title %}VentiRAD et AIO{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-4"></div>
            <div class="col-4">
                <h1>VentiRAD et AIO </h1>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row">
        <div class="col-4" id="colonne-1"></div>
            <div class="col 4" id="colonne-1">
                <br><br>
                <h2>Modifier :</h2>
            </div>
        </div>

        <div class="row">
            <div class="col-4" id="colonne-1"></div>
            <div class="col-4" id="colonne-1">
                {{form_start(formrefroidisseur)}}
                
                {{form_row(formrefroidisseur.nom, {'attr' : {'placeholder' : "Désignation du ventiRad / AIO"}})}}
                {{form_row(formrefroidisseur.marque, {'attr' : {'placeholder' : "Marque du ventiRad / AIO"}})}}
                {{form_row(formrefroidisseur.modele, {'attr' : {'placeholder' : "Modele du ventiRad / AIO"}})}}
                {{form_row(formrefroidisseur.supportcpu, {'attr' : {'placeholder' : " Support du processeur du ventiRad / AIO"}})}}
                {{form_row(formrefroidisseur.vitesserotationminimum, {'attr' : {'placeholder' : "Vitesse de rotation minimum du ventiRad / AIO"}})}}
                {{form_row(formrefroidisseur.vitesserotationmaximum, {'attr' : {'placeholder' : "Vitesse de rotation maximum du ventiRad / AIO"}})}}

                <button  type="submit" class="btn btn-success">
                        Enregistrer
                </button>

                {{form_end(formrefroidisseur)}}
                <br><br><br>
            </div>

            
        </div>
      </div> 

{% endblock %}

